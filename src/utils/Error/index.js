const ValidationErr = require("./Validation-error");

module.exports = {
    ServerErr: require("./server-error"),
    ValidationErr : require('./Validation-error'),
    ServiceErr : require("./service-error")
}